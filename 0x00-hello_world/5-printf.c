/*
 * Task 5: using the printf() function
 */

#include <stdio.h>

/**
 * main - prints the exact specified message
 * return - ends the code. always 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
